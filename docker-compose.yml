version: '3.8'

services:
  sop:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sop-system
    ports:
      - "80:3000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    networks:
      - sop-network

networks:
  sop-network:
    driver: bridge
